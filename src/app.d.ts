// See https://kit.svelte.dev/docs/types#app
// for information about these interfaces
declare global {
	namespace App {
		// The interface for custom error handling
		// Define the shape of errors thrown in your application
		// interface Error {
		//   message: string;
		//   code?: number;
		// }

		// The interface for custom local variables
		// Define properties that will be available in `load` functions and actions
		// interface Locals {
		//   user?: {
		//     id: string;
		//     name: string;
		//   };
		// }

		// The interface for page-specific data
		// Define the shape of the data available on each page
		// interface PageData {
		//   title: string;
		//   description: string;
		// }

		// The interface for page-specific state
		// Define the shape of the state that persists across navigation
		// interface PageState {
		//   scrollPosition: number;
		// }

		// The interface for platform-specific data
		// Define properties related to the platform, such as environment variables
		// interface Platform {
		//   env: {
		//     API_URL: string;
		//   };
		// }
	}
}

export { };
